package com.example.qrscanner;

import com.journeyapps.barcodescanner.CaptureActivity;

public class CaptureAct extends CaptureActivity {

    // You can add additional methods or customizations here if needed

    // No additional code is typically required in onCreate unless customizations are needed
    // Ensure the class extends CaptureActivity correctly as shown above

}
